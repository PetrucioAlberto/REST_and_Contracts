{
  "name": "entrega_aula_24",
  "version": "1.0.0",
 "scripts": {
  "test:report": "rimraf allure-results allure-report && cypress run --headless --browser chrome --env allure=true && allure generate allure-results --clean && allure open"
},
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.41.2",
    "allure-commandline": "^2.34.1",
    "cypress": "^15.6.0",
    "rimraf": "^6.1.0"
  },
  "dependencies": {
    "joi": "^18.0.1"
  }
}
